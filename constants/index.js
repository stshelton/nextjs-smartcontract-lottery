const contractAddresses = require("./contractAddresses.json")
const abi = require("./abi.json")

//exporting here allows u not to have to write of imports in js file that needs em
module.exports = {
    contractAddresses,
    abi,
}
